# ===========================================
# CONFIGURATION TEAMUP BACKEND POUR RENDER
# ===========================================

# Base de données MongoDB Atlas
MONGODB_URI=mongodb+srv://root:root@teamup.7fqehdy.mongodb.net/teamup?retryWrites=true&w=majority&appName=teamup

# Configuration JWT
JWT_SECRET=teamup_secret_key_production_2024_change_this_in_real_production
JWT_EXPIRES_IN=24h
JWT_REFRESH_SECRET=teamup_refresh_secret_production_2024_change_this_too
REFRESH_TOKEN_EXPIRES_IN=7d
SESSION_SECRET=teamup_session_secret_change_in_production

# Configuration serveur pour Render
PORT=10000
NODE_ENV=production

# Frontend URLs pour CORS (Expo EAS Build)
FRONTEND_URL=https://your-expo-app-url.expo.dev
FRONTEND_URLS=https://expo.dev,https://exp.host,https://snack.expo.io,exp://localhost:8081

# Configuration email
EMAIL_USER=nassimblm12@gmail.com
EMAIL_PASSWORD=wnivttwzatexdyje

# Configuration OAuth Google (optionnel)
# GOOGLE_CLIENT_ID=your_google_client_id
# GOOGLE_CLIENT_SECRET=your_google_client_secret

# ===========================================
# INSTRUCTIONS POUR RENDER:
# ===========================================
# 1. Copiez ces variables dans les Environment Variables de Render
# 2. Remplacez les valeurs par vos vraies valeurs de production
# 3. Assurez-vous que PORT=10000 (ou laissez Render le gérer automatiquement)
# 4. Mettez NODE_ENV=production pour la production
# 5. Ajoutez vos vraies URLs Expo EAS dans FRONTEND_URLS
# =========================================== 